# Check whether ONNX Runtime is loaded

Check whether ONNX Runtime is loaded

## Value

`TRUE` if the ONNX Runtime shared library has been loaded in the current
R session, `FALSE` otherwise.

## Examples

``` r
onnx_is_loaded()
#> [1] TRUE
```
