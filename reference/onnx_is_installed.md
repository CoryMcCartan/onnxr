# Check whether ONNX Runtime is available

Check whether ONNX Runtime is available

## Usage

``` r
onnx_is_installed()
```

## Value

`TRUE` if the ONNX Runtime shared library can be found in any of the
standard search locations, `FALSE` otherwise.

## Examples

``` r
onnx_is_installed()
#> [1] TRUE
```
