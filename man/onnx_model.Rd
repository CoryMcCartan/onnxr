% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{onnx_model}
\alias{onnx_model}
\title{Load an ONNX model}
\usage{
onnx_model(
  path,
  backend = c("cpu", "coreml", "cuda", "xnnpack", "openvino"),
  cache_dir = tools::R_user_dir("onnxr", "cache"),
  threads = 1L,
  opt_level = 99L
)
}
\arguments{
\item{path}{Path to an \code{.onnx} model file.}

\item{backend}{Execution backend. Available options depend on the
platform and ORT build:
\itemize{
\item \code{"cpu"} — Default, available everywhere.
\item \code{"coreml"} — Apple Neural Engine + CPU (macOS/iOS only).
\item \code{"cuda"} — NVIDIA GPU (Linux x64 and Windows x64 only). Requires
CUDA toolkit and the CUDA-enabled ORT build from
\code{onnx_install(cuda = TRUE)}.
\item \code{"xnnpack"} — Optimized CPU kernels (mobile/embedded). Requires
an ORT build with XNNPACK support (not provided by \code{\link[=onnx_install]{onnx_install()}}).
\item \code{"openvino"} — Intel hardware acceleration. Requires OpenVINO
installation and ORT build with OpenVINO EP (not provided by \code{\link[=onnx_install]{onnx_install()}}).
}}

\item{cache_dir}{Optional directory for CoreML model cache. Set to \code{NULL} to disable caching.}

\item{threads}{Number of threads. \code{0} uses all available;
a positive integer sets a fixed thread count.}

\item{opt_level}{Graph optimization level. \code{99} (default) enables all
optimizations; \code{1} for basic only; \code{0} to disable.}
}
\value{
An \code{"onnx_model"} object (a named list) with model metadata
and internal pointers used by \code{\link[=onnx_run]{onnx_run()}}.
}
\description{
Loads an \code{.onnx} model file and creates a model object.
}
\examples{
\donttest{
model_path <- system.file("extdata", "lm_iris.onnx", package = "onnxr")
if (onnx_is_loaded() && nzchar(model_path)) {
    sess <- onnx_model(model_path)
    sess
}
}
}
