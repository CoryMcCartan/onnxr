% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hlo.R
\name{onnx_to_hlo}
\alias{onnx_to_hlo}
\title{Convert an ONNX model to a StableHLO function}
\usage{
onnx_to_hlo(path, batch_size = 1L)
}
\arguments{
\item{path}{Path to an \code{.onnx} model file.}

\item{batch_size}{Integer batch size to use for dynamic dimensions.
StableHLO requires static shapes, so dynamic ONNX dimensions (marked
as -1) are replaced with this value. Defaults to \code{1L}.}
}
\value{
A \code{stablehlo} \code{Func} object representing the model computation.
}
\description{
Reads an \code{.onnx} model file and translates its computation graph into
StableHLO operations using the \code{stablehlo} R package. The resulting
function can be serialized with \code{\link[stablehlo:repr]{stablehlo::repr()}} and compiled with
\code{pjrt}.
}
\examples{
\dontrun{
func <- onnx_to_hlo(system.file("extdata", "lm_iris.onnx", package = "nativeORT"))
cat(stablehlo::repr(func))
}
}
